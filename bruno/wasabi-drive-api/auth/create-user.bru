meta {
  name: Create user
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/users
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "{{userName}}",
    "password": "{{userPassword}}"
  }
}