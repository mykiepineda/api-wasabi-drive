meta {
  name: Get user by id
  type: http
  seq: 4
  tags: [
    depends-on-create-user
    known-defect-flow
  ]
}

get {
  url: {{baseUrl}}/auth/users/{{userId}}
  body: none
  auth: none
}

tests {
  test("returns a user document", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
    expect(res.body).to.have.property("name");
  });
}