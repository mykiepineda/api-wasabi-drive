meta {
  name: List users
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/auth/users
  body: none
  auth: none
}

tests {
  test("returns a user list", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}