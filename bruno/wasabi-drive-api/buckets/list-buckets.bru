meta {
  name: List buckets
  type: http
  seq: 1
  tags: [
    regression
  ]
}

get {
  url: {{baseUrl}}/buckets/
  body: none
  auth: none
}

tests {
  test("returns a successful bucket list", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
    expect(res.body).to.have.property("Buckets");
  });
}